{
  "name": "@new-qp3d/pipeline",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "pipe-network": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run tests"
  },
  "dependencies": {
    "@new-qp3d/shared": "workspace:*",
    "pg": "^8.22.0",
    "pg-cursor": "^2.21.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
